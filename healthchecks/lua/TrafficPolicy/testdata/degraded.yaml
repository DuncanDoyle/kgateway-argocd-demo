# Real capture (kubectl get -o yaml) from a live kgateway v2.4.5 cluster.
# generation:7 is a leftover artifact of the Pending-reason watch probe, which
# patched this resource repeatedly to force reconciliations -- not hand-edited.
# observedGeneration matches it, so the status is a current verdict, and the
# spec below is exactly what the repo's manifest declares.
apiVersion: gateway.kgateway.dev/v1alpha1
kind: TrafficPolicy
metadata:
  generation: 7
  name: tp-degraded
  namespace: httpbin
spec:
  extAuth:
    extensionRef:
      name: no-such-gateway-extension
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: httpbin-via-service
status:
  ancestors:
    - ancestorRef:
        group: gateway.networking.k8s.io
        kind: Gateway
        name: demo-gateway
        namespace: httpbin
      conditions:
        - lastTransitionTime: "2026-09-24T09:08:38Z"
          message: 'extauth: httpbin/no-such-gateway-extension: gateway extension not found'
          observedGeneration: 7
          reason: Invalid
          status: "False"
          type: Accepted
        - lastTransitionTime: "2026-09-24T09:08:38Z"
          message: ""
          observedGeneration: 7
          reason: Pending
          status: "False"
          type: Attached
      controllerName: kgateway.dev/kgateway
